StylesPath = .vale/styles
MinAlertLevel = suggestion

[*]
BasedOnStyles = Vale
; spelling is covered by nvim built-in :set spell + harper (both share
; spell/en.utf-8.add); Vale.Spelling has its own dictionary and would
; false-positive on every allowlisted domain term (EC2, brazil, ...)
Vale.Spelling = NO
