git/gitconfig.local.symlink

zprezto.symlink
.kiro/agents/builder.json
private-dotfiles
work-dotfiles

# `home-manager build` / `nix build` output symlinks. Targets are machine-
# specific (/nix/store hashes), so committing them just churns history with
# unreproducible refs.
/result
/result-*
*.pyc
